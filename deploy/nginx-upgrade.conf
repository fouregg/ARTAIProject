# Карта для апгрейда до websocket: обычный запрос закрывает соединение,
# запрос с Upgrade — сохраняет. Нужна экрану цифрового холста.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
